# hyprwhspr

hyprwhspr project


## Conventions

- **Package Manager**: uv (fast Python package manager)
- **Formatting**: ruff format (Black-compatible)
- **Linting**: ruff check (fast, Flake8-compatible)
- **Testing**: pytest with fixtures in `tests/`
- **Config**: `pyproject.toml` for all project settings
